name: My-React-App

on:
  workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
          - name: checkout code 
            uses: actions/checkout@v3

          - name: Install Dependencies
            run: echo 'this is a test'

          #test


            
